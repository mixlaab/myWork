def solution(S):
    # write your code in Python 3.6
    stack = [1,2,3]
    comando = "A"
    for i in range(len(S)):
        if S[i] == " ":
            if (comando == DUP)
                stack.extend(stack[len(stack)-1])
            comando = ""
        else:
            comando = comando +  S[i]
        
    print(comando)
    pass

solution("DUP")
print (stack)


