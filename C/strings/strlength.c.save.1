#include<stdio.h>
#include<cs50.h>
#include<string.h>

int main(void){
	string name = get_string("Type your name, please: ");
	printf("Hello %s\n", name);
	int num;
	while (num = 0; num < strlen(name); num++){
		printf("%c\n", name[num]);
	}
	printf("Your name has %i letters\n", num);
}

